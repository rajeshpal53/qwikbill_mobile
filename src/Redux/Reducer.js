import {
  ADD_ITEMS_TOTALPRICE,
  ADD_TO_CART,
  REMOVE_FROM_CART,
  RESET_CARTITEMS,
} from "./Constants";

const initialState = {
    
}

export const Reducer = (state = initialState, action) => {
  return 0;
};
